[project]
name = "odmr-gui"
version = "0.1.0"
description = "Lightweight local GUI for odmr runtime"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.7",
  "pyqtgraph>=0.13",
]

[project.scripts]
odmr-gui = "odmr_gui.app:main"
