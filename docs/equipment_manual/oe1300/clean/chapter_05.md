# 5. 远程编程

## 5.1 OE1300 命令语法

上位机与 OE1300 的通信使用 ASCII 字符来进行。命令符使用大写，所有命令均由四个命令字符（如有必要可带上参数）和一个命令终结符组成。终结字符必须是一个回车符 `<cr>`。OE1300 只有在收到命令终结符时，才会执行用户输入的命令。命令可能需要一个或多个参数，多个参数之间用逗号分隔。

多个命令可以在同一命令行发送，但命令之间需要添加分号 `;`。

OE1300 有一个容量为 64 byte 字符的输入缓存区，并根据接收命令的顺序来处理命令。当缓存区写满时，最新命令将会把最旧并已执行命令覆盖。建议一次输入多个命令时不要超过 64 byte 字符。

OE1300 允许用户通过命令查询内部参数的当前值。查询命令的格式为由当前命令后加上一个问号 `?` 并省略原命令所需的一个或多个参数。OE1300 以 ASCII 字符串的形式返回用户所查询的参数，如果一个命令行中发送多个查询（用分号隔开）的话，应答将会按顺序一个一个地返回，每个返回值后尾都跟着一个终结符。

命令格式举例：

| 命令 | 说明 |
|------|------|
| `FMOD 1 <cr>` | 设置参考源为内部参考 |
| `FREQ 10E3 <cr>` | 设置内部参考信号频率为 10 kHz |
| `OUTP? 1 <cr>` | 查询通道 1 的输出值 |

## 5.2 详细的命令列表

每一个命令所指定的参数是有严格顺序的，不同参数之间用逗号 `,` 分隔。在大括号 `{ }` 里面的参数是可选的，不需要每个都填写。只有在命令后面加上 `(?)` 的助记符时，才会启动查询命令，没有 `(?)` 是不会查询的。大部分情况下，查询命令不需要发送大括号 `{ }` 内的内容。注意：在发送命令时 `()` 和 `{}` 都不需要发送。

变量定义如下：

| 变量 | 类型 |
|------|------|
| `i, j, k, l, m, n, o, p, q, r, s, t, u` | 整数 |
| `x` | 实数 |
| `f` | 频率值 |

以上所有的数值变量均可以被表示为整数、浮点数或指数格式（例如，数字 5 可以表示为 `5`、`5.0`、`0.5E1`）。而字符串则被作为一个 ASCII 字符序列的形式发送。

### 5.2.1 输入方式指令

| 命令 | 说明 |
|------|------|
| `ISRC (?) {i}` | `ISRC` 指令用于设置或查询输入信号的方式。<br>`i = 0` 时选择 A（单端电压信号输入）；<br>`i = 1` 时选择 `<A-B>`（差分电压输入）；<br>`i = 2` 时选择 `<I>`（电流信号输入）。 |
| `ICPL (?) {i}` | `ICPL` 指令用于设置或查询输入耦合方式。<br>`i = 0` 时选择 `<AC>`（交流耦合输入）；<br>`i = 1` 时选择 `<DC>`（直流耦合输入）。 |

### 5.2.2 范围与时间常数指令

| 命令 | 说明 |
|------|------|
| `IRNG (?) {i}` | `IRNG` 指令用于设置或查询系统量程 `<input range>`。参数 `i` 用于选择不同的量程，具体如下： |

| i | 量程 | i | 量程 |
|---|---|---|---|
| 0 | 5 V/uA | 15 | 50 uV/pA |
| 1 | 2 V/uA | 16 | 20 uV/pA |
| 2 | 1 V/uA（default） | 17 | 10 uV/pA |
| 3 | 500 mV/nA | 18 | 5 uV/pA |
| 4 | 200 mV/nA | 19 | 2 uV/pA |
| 5 | 100 mV/nA | 20 | 1 uV/pA |
| 6 | 50 mV/nA | 21 | 500 nV/fA |
| 7 | 20 mV/nA | 22 | 200 nV/fA |
| 8 | 10 mV/nA | 23 | 100 nV/fA |
| 9 | 5 mV/nA | 24 | 50 nV/fA |
| 10 | 2 mV/nA | 25 | 20 nV/fA |
| 11 | 1 mV/nA | 26 | 10 nV/fA |
| 12 | 500 uV/pA | 27 | 5 nV/fA |
| 13 | 200 uV/pA | 28 | 2 nV/fA |
| 14 | 100 uV/pA | 29 | 1 nV/fA |

| 命令 | 说明 |
|------|------|
| `OFLT (?) {x}` | `OFLT` 指令用于设置或查询滤波器的时间常数。<br>参数 `x` 代表时间常数，单位 s，取值范围 `1E-6 ~ 3000`，`0.01`（default）。 |
| `OFSL (?) {i}` | `OFSL` 指令用于设置或查询低通滤波器的陡降。<br>参数 `i` 用于选择不同陡降，具体如下： |

| i | Filter dB/oct |
|---|---|
| 0 | 6 dB/oct |
| 1 | 12 dB/oct（default） |
| 2 | 18 dB/oct |
| 3 | 24 dB/oct |
| 4 | 30 dB/oct |
| 5 | 36 dB/oct |
| 6 | 42 dB/oct |
| 7 | 48 dB/oct |

| 命令 | 说明 |
|------|------|
| `SYNC (?) {i}` | `SYNC` 指令用于设置或查询同步滤波器的开关状态。一般当参考频率低于 1000 Hz 时需要开启同步滤波器。<br>同步滤波器只有低通滤波器陡降 $\ge 18\ \mathrm{dB/oct}$ 且参考频率低于 1 kHz 时有效。<br>`i = 0`：关闭同步滤波器（default）；<br>`i = 1`：开启同步滤波器。 |

### 5.2.3 参考与相位指令

| 命令 | 说明 |
|------|------|
| `PHAS (?) i, {x}` | `PHAS` 指令用于设置或查询解调通道相移。<br>参数 `i` 是解调通道数，取值 `0~7`，`0` 代表基波，`1~7` 代表谐波通道；<br>参数 `x` 是指相位值（实数值，单位：°）。<br>相位限制在 $\pm 180°$。 |
| `FMOD (?) {i}` | `FMOD` 指令用于设置或查询参考信号源。<br>`i = 0` 时选择外部参考；<br>`i = 1` 时选择内部参考；<br>`i = 2` 时选择输入自参考信号。 |
| `FREQ (?) {f}` | `FREQ` 指令用于设置或查询内部参考信号的频率，`f` 值可设置范围 `1 mHz ~ 100 kHz`，最小分辨率为 `1 mHz`。指令 `FREQ?` 会返回当前的参考信号频率（内部或外部）。 |
| `RSLP (?) {i}` | 当使用外部参考源时，`RSLP` 指令用于设置或查询参考信号当前的触发方式。<br>`i = 0` 时设置 `<TTL>` 上升沿触发；<br>`i = 1` 时设置 `<TTL>` 下降沿触发；<br>`i = 2` 时设置正弦波过零检测 `<Sine>`。<br>当频率低于 2 Hz 时，须使用 `<TTL>` 触发方式。 |
| `DMOD (?) i {, j}` | `DMOD` 指令用于设置或查询 7 个解调器的模式。<br>参数 `i` 必须设置，取值 `0~6`：`i = 0` 时选择解调器 1，`i = 1` 时选择解调器 2，……，`i = 6` 时选择解调器 7；<br>参数 `j` 用于设置解调器的模式：`j = 0` 时选择谐波模式，`j = 1` 时选择任意频率解调模式。<br>指令 `DMOD? i` 会返回当前查询的解调器的解调模式。 |
| `HARM (?) i {, j}` | `HARM` 指令用于设置或查询谐波阶数。<br>参数 `i` 必须设置，取值 `0~6` 之间的整数：`i = 0` 时选择解调器 1，`i = 1` 时选择解调器 2，……，`i = 6` 时选择解调器 7；<br>参数 `j` 可以设置为 `1 ~ 65535` 之间的整数，表示谐波阶数。<br>`HARM i, j` 指令将会设置检测输入参考频率的 `j` 次谐波。参数 `j` 必须满足 $j \times f \le 100/500\ \mathrm{kHz}$。如果 `j` 次谐波的值大于 $100/500\ \mathrm{kHz}$，那么谐波次数 `j` 会被自动设置为满足条件 $j \times f \le 100/500\ \mathrm{kHz}$ 的 `j` 的最大值。 |
| `DARB (?) i {, f}` | `DARB` 指令用于设置或查询解调器的任意频率模式时的参考频率。<br>参数 `i` 必须设置，取值 `0~6` 之间的整数：`i = 0` 时选择解调器 1，`i = 1` 时选择解调器 2，……，`i = 6` 时选择解调器 7；<br>参数 `f` 值可设置范围 `1 uHz ~ 100/500 kHz`，最小分辨率为 `1 uHz`。 |

### 5.2.4 正弦波输出指令

| 命令 | 说明 |
|------|------|
| `SWVT (?) {i}` | `SWVT` 指令用于设置或查询 `<Sine Output>` 的输出类型。<br>`i = 0` 时选择关闭 Sineout 和 TTL out 输出；<br>`i = 1` 时选择开启 Sineout 和 TTL out 输出。 |
| `SLVL (?) {x}` | `SLVL` 指令用于设置或查询输出的同步正弦波固定幅值模式的幅度。<br>参数 `x` 指幅度电压（实数值，单位：V）。参数 `x` 必须满足 $100\ \mathrm{\mu V_{rms}} \le x \le 5\ \mathrm{V_{rms}}$，最小分辨率为 $10\ \mathrm{\mu V_{rms}}$。 |

### 5.2.5 CH 通道输出指令

| 命令 | 说明 |
|------|------|
| `COUT (?) i {, j}` | `COUT` 指令用于设置或查询 AUXOUT 输出通道。<br>发送该指令时参数 `i` 必须设置：`i = 0` 时选择 CH1，`i = 1` 时选择 CH2；<br>参数 `j` 用于选择输出值的类型，具体如下： |

| j | CH 通道源 | j | CH 通道源 |
|---|---|---|---|
| 0 | FIXED | 19 | RD4 |
| 1 | X | 20 | $\theta$D4 |
| 2 | Y | 21 | XD5 |
| 3 | R | 22 | YD5 |
| 4 | $\theta$ | 23 | RD5 |
| 5 | XD1 | 24 | $\theta$D5 |
| 6 | YD1 | 25 | XD6 |
| 7 | RD1 | 26 | YD6 |
| 8 | $\theta$D1 | 27 | RD6 |
| 9 | XD2 | 28 | $\theta$D6 |
| 10 | YD2 | 29 | XD7 |
| 11 | RD2 | 30 | YD7 |
| 12 | $\theta$D2 | 31 | RD7 |
| 13 | XD3 | 32 | $\theta$D7 |
| 14 | YD3 | 33 | X-Noise |
| 15 | RD3 | 34 | Y-Noise |
| 16 | $\theta$D3 | 35 | Frequency |
| 17 | XD4 | 36 | AUX-IN1 |
| 18 | YD4 | 37 | AUX-IN2 |

| 命令 | 说明 |
|------|------|
| `CAUX (?) i {, x}` | `CAUX` 指令用于设置或查询 AUXOUT 输出的电压值。<br>参数 `i` 对应 CH 的通道：`i = 0` 时对应 CHOUT1，`i = 1` 时对应 CHOUT2；<br>参数 `x` 用于设置 DAC 的电压值，范围是 $-10.000\ \mathrm{V} \le x \le 10.000\ \mathrm{V}$。<br>例如发送指令 `CAUX 1,5.00`，会设置 `<CHOUT2>` 的 AUXOUT 模式的输出值为 `5.00 V`。 |
| `COFP (?) i {, x}` | `COFP` 指令用于设置或查询 CHOUT 通道输出的偏置值。<br>参数 `i` 对应 CH 的通道：`i = 0` 时对应 CHOUT1，`i = 1` 时对应 CHOUT2；<br>参数 `x` 用于设置偏置值，范围是 $-100.00 \le x \le 100.00$（%），最小分辨率为 `0.01`。 |
| `CEXP (?) i {, j}` | `CEXP` 指令用于设置或查询 CHOUT 通道输出的放大倍数值。<br>参数 `i` 对应 CH 的通道：`i = 0` 时对应 CHOUT1，`i = 1` 时对应 CHOUT2；<br>参数 `j` 用于设置输出放大倍数，范围是 $0.001 \le x \le 10000$ 的整数。 |

### 5.2.6 PID 设置

| 命令 | 说明 |
|------|------|
| `*PID (?) i, j {, x}` | 其中 `i` 代表配置哪个通道，`j` 代表 PID 的某个功能的编码，`x` 是具体的设置参数。<br>`i`：PID 通道，`i = 0` 对应通道 1，`i = 1` 对应通道 2；<br>`j`（PID 功能选项地址）和 `x`（PID 参数）的取值及其含义见下表： |

| j | 功能说明及 x 取值 |
|---|---|
| 0 | 保留 / Reserve |
| 1 | 使能 / 关闭 PID 模块：<br>`x = 0`：关闭 PID 模块；<br>`x = 1`：使能 PID 模块。 |
| 2 | 输入源选择 / Input Source Select，可以选择所有 8 个锁放解调通道的输出 X、Y、R、Xita 或者 Aux IN1、Aux IN2 等。<br>`x` 取值范围为 `0~31` 以及 `34~36`，对应参数见 `SNAP` 指令的参数。 |
| 3 | 输出目的地选择。<br>`x` 取值范围：`0~3`；<br>`x = 0`：Aux Out1；<br>`x = 1`：Aux Out2；<br>`x = 2`：Sine Out；<br>`x = 3`：Int Frequency。 |
| 4 | 采样间隔（通过采样间隔设置采样率）。<br>`x` 取值范围：`0~31`（整数）；<br>Sample Rate = $4\ \mathrm{MHz} / 2^x$；<br>举例：<br>`x = 0` 时，Sample Rate = $4\ \mathrm{MHz}$；<br>`x = 1` 时，Sample Rate = $2\ \mathrm{MHz}$；<br>……<br>`x = 31` 时，Sample Rate = $1.86\ \mathrm{mHz}$。 |
| 5 | 保留 / Reserve |
| 6 | 系数 $K_p$，`x` 取值范围：实数。 |
| 7 | 系数 $K_i$，`x` 取值范围：实数。 |
| 8 | 系数 $K_d$，`x` 取值范围：实数。 |
| 9 | 微分项滤波器带宽 Filter Bandwidth（Hz）。<br>带宽设置需要满足：<br>Filter Bandwidth $<$ Sample Rate $/ \pi$。<br>否则滤波器不起作用，也即是说当不需要应用滤波器时，设置 Filter Bandwidth $\ge (1/\pi) \times$ Sample Rate 即可。 |
| 10 | 输出限幅 Max Output |
| 11 | 输出限幅 Min Output |
| 12 | Set Point |
| 13 | Output Offset |
| 14 | 积分项限幅 Max Integral |
| 15 | 积分项限幅 Min Integral |
| 16 | PID 输出值，只可查询，不可设置 |
| 17 | PID 输入值，只可查询，不可设置 |
| 18 | 保留 / Reserve |

### 5.2.7 波特率设置

| 命令 | 说明 |
|------|------|
| `BAUD (?) {j}` | `BAUD` 指令用于设置或查询串口波特率。<br>参数 `j` 对应波特率值。<br>参数 `j` 值可设置：`9600`、`115200`（default）、`921600`。 |

### 5.2.8 网口参数设置

| 命令 | 说明 |
|------|------|
| `NMOD (?) {i}` | `NMOD` 指令用于设置或查询网口连接模式。参数 `i` 值可设置：<br>`i = 0` 时选择 TCP 模式；<br>`i = 1` 时选择 DHCP 模式。 |
| `NIPA (?) {s}` | `NIPA` 指令用于设置或查询网口的 IP 地址。<br>参数 `s` 值格式为：`x:x:x:x`，取值范围：`0~255`。<br>默认值为 `192.168.1.1`。<br>例如发送指令 `NIPA 192.168.1.5`，会设置网口的 IP 地址为 `192.168.1.5`。 |
| `NSMA (?) {s}` | `NSMA` 指令用于设置或查询网口的子网掩码。<br>参数 `s` 值格式为：`x:x:x:x`，取值范围：`0~255`。<br>默认值为 `255.255.255.0`。 |
| `NGWA (?) {s}` | `NGWA` 指令用于设置或查询网口的网关地址。<br>参数 `s` 值格式为：`x:x:x:x`，取值范围：`0~255`。<br>默认值为 `192.168.1.10`。 |

### 5.2.9 存读取设置指令

| 命令 | 说明 |
|------|------|
| `*RST` | `RST` 指令用于复位。设备内部的所有状态与参数都会重置为默认值，数据缓存区内的数据也会丢失。`*` 号是为了补齐 4 位命令字符。 |
| `*IDN ?` | `IDN?` 指令用于查询模块锁放的 ID，格式为 `SSI OE1300, SNXXXXXX, Version: VXXX`。其中第一个为型号，如 `OE1300`；第二个为序列号，如 `SN00001`；第三个为硬件版本号，如 `Version: V1.00`。`*` 号是为了补齐 4 位命令字符。 |

### 5.2.10 数据和状态读取指令

`RALL?` 指令用于读取所有的解调结果数据以及 Aux IN 数据。

参数的值以 ASCII 浮点数的格式返回，以伏特（V）、安培（A）、度（°）或赫兹（Hz）为单位，但单位不会输出。该指令仅用作查询。

返回的值是一个单一的字符串，该字符串内不同值之间用逗号 `,` 分隔，并且顺序如下：

| 数据顺序编码 | Parameter |
|---|---|
| 0 | X |
| 1 | Y |
| 2 | R |
| 3 | $\theta$ |
| 4 | XD1 |
| 5 | YD1 |
| 6 | RD1 |
| 7 | $\theta$D1 |
| 8 | XD2 |
| 9 | YD2 |
| 10 | RD2 |
| 11 | $\theta$D2 |
| 12 | XD3 |
| 13 | YD3 |
| 14 | RD3 |
| 15 | $\theta$D3 |
| 16 | XD4 |
| 17 | YD4 |
| 18 | RD4 |
| 19 | $\theta$D4 |
| 20 | XD5 |
| 21 | YD5 |
| 22 | RD5 |
| 23 | $\theta$D5 |
| 24 | XD6 |
| 25 | YD6 |
| 26 | RD6 |
| 27 | $\theta$D6 |
| 28 | XD7 |
| 29 | YD7 |
| 30 | RD7 |
| 31 | $\theta$D7 |
| 32 | X-Noise |
| 33 | Y-Noise |
| 34 | Frequency |
| 35 | AUX-IN1 |
| 36 | AUX-IN2 |

`SNAP? i,j {,k,l,m,n,o,p,q,r}` 指令用于在同一个时间点记录最多 10 个不同的参数值。

例如，指令 `SNAP?` 用于在同一时刻查询 `<X>`、`<Y>`、`<R>`、`<$\theta$>` 或 `<F>` 等值，该功能在时间常数很短的时候非常实用。因为如果使用 `OUTP?` 指令来连续读取两个不同的参数值，两个参数返回值之间会有一定的延时，使得读取的两个数据不是在同一时刻下测量所得，当数据变化速度较快时，就可能导致一定误差。

`SNAP? i,j` 指令需要至少 2 个参数，最多可以同时读取 10 个。参数 `i, j, k, l, m, n, o, p, q, r` 的选择具体如下：

| i, j, k, l, m, n, o, p, q, r | Parameter |
|---|---|
| 0 | X |
| 1 | Y |
| 2 | R |
| 3 | $\theta$ |
| 4 | XD1 |
| 5 | YD1 |
| 6 | RD1 |
| 7 | $\theta$D1 |
| 8 | XD2 |
| 9 | YD2 |
| 10 | RD2 |
| 11 | $\theta$D2 |
| 12 | XD3 |
| 13 | YD3 |
| 14 | RD3 |
| 15 | $\theta$D3 |
| 16 | XD4 |
| 17 | YD4 |
| 18 | RD4 |
| 19 | $\theta$D4 |
| 20 | XD5 |
| 21 | YD5 |
| 22 | RD5 |
| 23 | $\theta$D5 |
| 24 | XD6 |
| 25 | YD6 |
| 26 | RD6 |
| 27 | $\theta$D6 |
| 28 | XD7 |
| 29 | YD7 |
| 30 | RD7 |
| 31 | $\theta$D7 |
| 32 | X-Noise |
| 33 | Y-Noise |
| 34 | Frequency |
| 35 | AUX-IN1 |
| 36 | AUX-IN2 |

返回的值是一个单一的字符串，该字符串内不同值之间用逗号 `,` 分隔，并且顺序是按照发送指令时 `i, j, k, l, m` 的顺序返回。例如，发送 `SNAP?0,1,34,3;` 会依次返回 `<X>`、`<Y>`、`<Frequency>` 和 `<$\theta$>` 的值。这些值均放在同一个字符串中，例如：

```
"0.951359,0.0253297,1000.00,1.234"
```

第一个是 `<X>` 值，第二个是 `<Y>` 值，第三个是频率值，第四个是 `<$\theta$>` 值。

该指令对比 `OUTP` 指令好处是可以同时获取多个数据，这些数据都是同一时刻的，不会存在延时。

`OUTP? i` 指令用于读取单个参数值。参数 `i` 对应于下表：

| i | Parameter |
|---|---|
| 0 | X |
| 1 | Y |
| 2 | R |
| 3 | $\theta$ |
| 4 | XD1 |
| 5 | YD1 |
| 6 | RD1 |
| 7 | $\theta$D1 |
| 8 | XD2 |
| 9 | YD2 |
| 10 | RD2 |
| 11 | $\theta$D2 |
| 12 | XD3 |
| 13 | YD3 |
| 14 | RD3 |
| 15 | $\theta$D3 |
| 16 | XD4 |
| 17 | YD4 |
| 18 | RD4 |
| 19 | $\theta$D4 |
| 20 | XD5 |
| 21 | YD5 |
| 22 | RD5 |
| 23 | $\theta$D5 |
| 24 | XD6 |
| 25 | YD6 |
| 26 | RD6 |
| 27 | $\theta$D6 |
| 28 | XD7 |
| 29 | YD7 |
| 30 | RD7 |
| 31 | $\theta$D7 |
| 32 | X-Noise |
| 33 | Y-Noise |
| 34 | Frequency |
| 35 | AUX-IN1 |
| 36 | AUX-IN2 |

| 命令 | 说明 |
|------|------|
| `OAUX? i` | `OAUX` 指令用于查询 AUX-IN 接口的输入电压值。<br>参数 `i` 必须设置：<br>`i = 0` 时读取 AUX-IN1；<br>`i = 1` 时读取 AUX-IN2。<br>查询返回结果以伏特（V）为单位，但单位不会输出。 |
| `OVLD?` | `OVLD?` 指令用于查询 ADC Overload 的状态。<br>查询返回结果是 `0` 或者 `1`。<br>`0` 表示 ADC 没有溢出；<br>`1` 表示 ADC 发生溢出。此时需要把输入信号减小或者调整 input range 到更大量程。 |
| `*PLL?` | `*PLL?` 指令用于查询锁相环的状态。查询返回结果是 `0` 或者 `1`。<br>`0` 表示现在锁相环没有锁定或者是处于内部参考模式；<br>`1` 表示锁相环已经锁定。 |
